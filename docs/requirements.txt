# Docs-only dependencies. Install with:
#   pip install -r docs/requirements.txt
# or via the project extra:
#   pip install -e .[docs]
sphinx>=7.0
sphinx-rtd-theme>=2.0
myst-parser>=2.0
sphinx-copybutton>=0.5
tomli>=2.0; python_version < "3.11"
